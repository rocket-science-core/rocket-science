import React from "react";
import { render } from "@testing-library/react";
import SideNav from "./SideNav";

// it("SideNav renders text prop", () => {
//     const { getByText } = render(
//     <SideNav text={"Hello World from test"} />
//     );
//     expect(getByText("Hello World from test")).toBeTruthy();
// });

// it("SideNav renders with no prop value provided", () => {
//     const { getByText } = render(<SideNav text={""} />);
//     expect(getByText("no prop value provided")).toBeTruthy();
// });