import styled from 'styled-components';

const SomeExampleWrapper = styled.div`
    /* background-color: red;

    > .styled-button {
        background-color: blue;
    } */
`

export default SomeExampleWrapper;