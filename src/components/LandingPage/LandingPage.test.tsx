import React from "react";
import { render } from "@testing-library/react";
import LandingPage from "./LandingPage";

// it("LandingPage renders text prop", () => {
//     const { getByText } = render(
//     <LandingPage text={"Hello World from test"} />
//     );
//     expect(getByText("Hello World from test")).toBeTruthy();
// });

// it("LandingPage renders with no prop value provided", () => {
//     const { getByText } = render(<LandingPage text={""} />);
//     expect(getByText("no prop value provided")).toBeTruthy();
// });